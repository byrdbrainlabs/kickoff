<!--
  PLACEHOLDER — not yet generated.

  The documentation agent (agents/documentation.md) creates and maintains this
  index as features ship. Each row points to a feature doc in this directory.
  See agents/schemas/feature-doc.md for the index format.

  This directory is RUN STATE — it describes what an execution run has built.
  For a greenfield re-run of the backlog, reset it (index and feature docs)
  to this placeholder along with the product code.

  Agents treat this file as "not yet created" until it has real content
  (i.e., while it contains only this placeholder).
-->
